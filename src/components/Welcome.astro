---
const { title, tagline, ctaPrimaryHref, ctaPrimaryLabel, ctaSecondaryHref, ctaSecondaryLabel } = Astro.props;
---

<section class="py-16 sm:py-20">
  <div class="glass-strong p-8 sm:p-10">
    <h1 class="text-3xl font-bold tracking-tight sm:text-5xl">
      {title ?? "Modern web experiences, built clean."}
    </h1>
    <p class="mt-4 max-w-2xl text-sm text-white/75 sm:text-base">
      {tagline ?? "Responsive, bilingual (EL/EN), SEO-friendly portfolio with a focus on UI/UX and performance."}
    </p>

    <div class="mt-8 flex flex-wrap gap-3">
      <a class="btn-primary" href={ctaPrimaryHref ?? "#contact"}>{ctaPrimaryLabel ?? "Get in touch"}</a>
      <a class="btn-ghost" href={ctaSecondaryHref ?? "#projects"}>{ctaSecondaryLabel ?? "See projects"}</a>
    </div>
  </div>
</section>
