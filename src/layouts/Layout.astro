---
import BaseLayout from "./BaseLayout.astro";
const { title, description, lang, robots, canonicalPathname } = Astro.props;
---

<BaseLayout
  title={title}
  description={description}
  lang={lang}
  robots={robots}
  canonicalPathname={canonicalPathname}
>
  <slot />
</BaseLayout>
